const accounts=()=>JSON.parse(localStorage.getItem("tubalHubAccounts")||"[]");const save=a=>localStorage.setItem("tubalHubAccounts",JSON.stringify(a));const sf=document.getElementById("signupForm");if(sf)sf.onsubmit=e=>{e.preventDefault();let a=accounts(),email=signupEmail.value.trim().toLowerCase();if(a.some(x=>x.email===email)){signupMessage.textContent="Email already registered.";return}a.push({name:signupName.value.trim(),email,password:signupPassword.value});save(a);location.href="login.html"};
